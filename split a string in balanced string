class Solution(object):
    def balancedStringSplit(self, s):
        count=0
        res=0
        for c in s:
            if c=='L':
                count+=1
            elif c=='R':
                count-=1
            if count ==0:
                res+=1
        return res
